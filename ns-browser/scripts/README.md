# Script injection

Use a conservative allowlist for site-specific scripts.
Only inject scripts when the user has permission to modify the target content.
Keep injection disabled by default for unknown sites.
