# AOSP WebView provider scaffold

Package: `io.github.nsbrowser.webview`

A production WebView provider is not simply a second ordinary browser APK.
Android WebView provider selection and privileged/system integration depend on
the Android/AOSP build and device configuration.

This folder documents the intended second target while keeping the main browser
build independent.
